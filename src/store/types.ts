// src/store/types.ts
/**
 * Tipos mínimos para compatibilidad con selectors.ts
 */

import type { RanchOSStore } from './index';

// Alias para mantener compatibilidad con el código existente
export type RanchOSState = RanchOSStore;