// AnnualPerformanceAnalyzer/localizer.ts
import { LocaleConfig } from '../../../types/base';

export class AnnualPerformanceAnalyzerLocalizer {
  async localize(data: any, config: LocaleConfig): Promise<any> {
    return data;
  }
}