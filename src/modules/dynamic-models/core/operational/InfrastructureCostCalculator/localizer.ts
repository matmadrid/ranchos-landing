// InfrastructureCostCalculator/localizer.ts
import { LocaleConfig } from '../../../types/base';

export class InfrastructureCostCalculatorLocalizer {
  async localize(data: any, config: LocaleConfig): Promise<any> {
    return data;
  }
}